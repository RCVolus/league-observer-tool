{
  "name": "league-observer-tool-loader",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "start": "sirv public --no-clear",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  }
}
